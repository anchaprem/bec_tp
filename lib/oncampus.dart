import 'package:flutter/material.dart';

class OnCampusPage extends StatelessWidget {
  const OnCampusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("On-Campus Opportunities")),
      body: const Center(child: Text("On-Campus Page Content")),
    );
  }
}
